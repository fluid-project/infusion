if(!dojo._hasResource["tests.back"]){
dojo._hasResource["tests.back"] = true;
dojo.provide("tests.back");
if(dojo.isBrowser){
	doh.registerUrl("tests.back", dojo.moduleUrl("tests", "back.html"));
}

}
